<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ir.punshop.book">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="activities.SignUpActivity"></activity>
        <activity android:name="activities.LoginActivity" />
        <activity android:name="activities.InOrUpActivity" />
        <activity android:name="activities.MyAdsActivity" />
        <activity android:name="activities.ShowInfoActivity" />
        <activity android:name="activities.KindWallActivity" />
        <activity android:name="activities.SearchActivity" />
        <activity android:name="activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="activities.MainActivity"
            android:label="@string/title_activity_main" />
    </application>

</manifest>